# {{PROJECT_NAME}} — Agent Operating Manual

{{PROJECT_NAME}} is {{STACK_DESCRIPTION}}.

This file orients automated agents (Claude Code, CI bots, code-review agents) working on the codebase. It describes the mandatory development discipline and how to run the test suites locally.

---

## Branch discipline (mandatory)

> Every implementation MUST be done on a new branch created from `main` (or `master`). Never commit implementation changes directly to `main`/`master`.
>
> This ensures that in-progress code does not break the main branch and that every change passes CI before being integrated.

Standard flow before any change:

1. `git checkout main && git pull` — start from the latest state.
2. `git checkout -b <type>/<short-description>` — e.g. `feat/two-step-upload`, `fix/null-pointer`, `refactor/key-normalizer`.
3. Implement following TDD (see section below).
4. Open PR — CI runs mandatory jobs; merge only after green.

Exceptions (direct commit permitted only for):

- Edits to `AGENTS.md`, `README.md`, or other purely-documentary `*.md` files.
- Editor/ignore-file configuration tweaks with no runtime effect.

Any change under source or test directories, build configuration, migrations, or CI workflows **requires** a dedicated branch.

---

## Test-Driven Development (mandatory)

> Every new feature, bugfix, or behavior change MUST follow Red → Green → Refactor:
> 1. **Red** — write a failing test that demonstrates the desired behavior or reproduces the bug.
> 2. **Green** — write the minimum production code to make the test pass.
> 3. **Refactor** — clean up while keeping the bar green.
>
> A change under source files without a matching test change is a TDD violation — audit any diff with the `tdd-evidence-checker` review agent.

How to run tests:

{{TEST_COMMANDS}}

---

## Coverage thresholds

Coverage is enforced progressively. The current phase determines the bar that CI fails under.

{{COVERAGE_TARGETS}}

Thresholds are enforced via the test runner's coverage configuration. Bumping the bar happens in the same commit that closes a phase.

---

## When NOT to use TDD

Be honest: not every change needs a test.

- Pure dependency bumps (lockfile-only changes) — no.
- Documentation-only changes (`*.md`, `README`, `docs/`) — no.
- Pure tooling/config tweaks that have no runtime effect (e.g. editor settings, ignore-file additions) — no.

Anything that touches behavior — a route, a hook, a domain method, a schema, a model, a component's render output, a background task — **does** need a test. If you're unsure, write one.

---

## Pre-commit and CI

- **Pre-commit** (`.pre-commit-config.yaml`) is the local quick gate: linting and formatting on all source files, plus a fast unit-only test run filtered by changed files. Install with `pre-commit install` once per clone.
- **CI** is authoritative. It runs the test and lint jobs on every PR and push to `main`/`master`; no PR merges without green CI.

If pre-commit and CI disagree, **CI wins** — pre-commit may be skipped or run a narrower subset, but no PR merges without green CI.

**The gate is the integration suite.** Unit tests mock the DB/serialization/HTTP boundaries and cannot catch ORM↔domain mismatches, schema regressions, or migration failures. Before claiming any non-trivial change done, run the full integration suite. Also run the full frontend suite if applicable.

---

## Task schema

A plan is an ordinary Markdown document with `## Task N` (or `### Task N`) headings. To make a task machine-verifiable, **augment** it with a `task-meta` HTML-comment block declaring `id`, `touches`, `depends`, `verify`, and optionally `acceptance`. `run_task.py` then runs that task's `verify` and ticks its checkbox; a task with no `task-meta` block is human-run and ignored by the harness. It is an augmentation of a normal plan, not a separate plan format. See [`docs/task-schema.md`](docs/task-schema.md).

Validate any plan locally with:

```bash
python scripts/harness/validate_plan.py docs/plans/<file>.md
```

---

## Harness tooling

Five scripts extend the execution harness. All exit `0` on success, `1` on a real failure, `2` on a usage error. Plans live in `docs/plans/`.

- **Parallel scheduler** — `python scripts/harness/plan_schedule.py <plan.md>`
  prints the topological *layers* of a plan (tasks in the same layer have no dependency between them and may run in parallel) and fails if two same-layer tasks declare the same path in `touches`.
- **Doc-freshness check** — `python scripts/harness/check_freshness.py <file.md> …`
  flags expired `<!-- freshness: YYYY-MM-DD ttl: Nd -->` markers. Annotate any dated claim with a marker; the check fails once `date + ttl` is in the past. Runs as the advisory `doc-freshness` pre-commit hook.
- **Cycle closer** — `python scripts/harness/cycle_done.py --plan <file>`
  closes a work cycle: verifies all tasks are checked off **and** every command in `.harness/gates` exits 0, then appends an `[Unreleased]` entry to `CHANGELOG.md`. `.harness/gates` holds one shell command per line (the project's test / lint / typecheck / build commands). To bypass with an audit trail, use `--force -m "<reason>"` (logged to `.harness/exceptions.log`).
- **Plan validator** — `python scripts/harness/validate_plan.py <plan.md>`
  validates task-meta blocks for schema correctness, unique ids, dependency cycles, and write-collision conflicts.
- **Task runner** — `python scripts/harness/run_task.py <plan.md> <task-id>`
  runs a task's `verify` command and ticks its checkbox in the plan file on success.

A cycle is "done" only when `scripts/harness/cycle_done.py --plan <file>` exits 0.

---

## Package layout

{{PACKAGE_LAYOUT}}

---

<!-- OPTIONAL:DOMAIN_RULES -->
## Domain rules (mandatory)

{{DOMAIN_RULES}}
<!-- /OPTIONAL:DOMAIN_RULES -->

<!-- OPTIONAL:UI_RULES -->
## UI rules (mandatory)

{{UI_RULES}}
<!-- /OPTIONAL:UI_RULES -->

<!-- OPTIONAL:ANTAGONIST_CRITICS -->
## Antagonist critics on specs and plans (mandatory when configured)

> **Source of truth:** `.harness/critics.json`. If that file is absent or
> lists no models, this protocol is disabled — delete the file to turn the
> feature off; edit its `models` list to change the critics.

At the end of every spec creation and every plan creation — **before**
asking the user to review the document — dispatch one `antagonist-critic`
subagent per model listed in `.harness/critics.json`, in parallel, using
the Agent tool's `model` override. Point each critic at the document path.
Their sole job is to refute it.

- **One round per presented version.** Substantial edits after a critique
  round re-trigger it; the resolution edits themselves do not.
- **Resolution:** fix every objection you can in the document. For each
  objection you do not fix, record the justification. Present the user the
  refined document plus a criticism → response summary.
- **Failure:** if a critic dispatch fails (model unavailable), say so in
  the summary and proceed with the critics that ran. If ALL critics fail,
  tell the user explicitly that no adversarial review ran — never present
  the document as if it had been critiqued.
- **Scope:** plans live in `docs/plans/`; specs are any `*.md` under a
  `specs/` directory (projects without a spec workflow simply never
  exercise that half). If the session model equals a configured critic
  model, the independence benefit shrinks to fresh-context adversarial
  framing — still run it.
<!-- /OPTIONAL:ANTAGONIST_CRITICS -->

<!-- OPTIONAL:ARCHITECTURE -->
## Architecture

Architecture leash not yet configured — run `compose-architecture-leash` to
set it up. Once configured, the structured spec lives in
[`.harness/architecture.yaml`](.harness/architecture.yaml) and this section
is regenerated from it. The `<!-- OPTIONAL:ARCHITECTURE -->` markers above
and below this paragraph are kept on purpose: the compiler finds its
territory by those markers, so do not delete them.
<!-- /OPTIONAL:ARCHITECTURE -->

### Parallel work (worktrees)

Branch discipline is mandatory; worktrees are the **opt-in** ergonomics that
make N mandatory branches livable at once. To start a change in its own
working directory without the stash-dance, invoke `/leash-start-work` (or, by
hand, the same convention):

```bash
git worktree add .worktrees/<slug> -b <type>/<slug> main   # type ∈ feat|fix|refactor|docs|chore
```

This is the *physical* form of "branch first, edit after": one folder per
branch, branched from `main`. It does not replace the rule — it makes it
comfortable. When the branch is merged, run `git worktree remove
.worktrees/<slug>`; `cycle_done.py` prints an advisory reminder once it is
merged. The worktree leash below makes this the *only* way to write — the main tree is read-only.

### Worktree leash (main is read-only)

A `PreToolUse` gate denies writes (`Edit`/`Write`/`MultiEdit`/`NotebookEdit`)
whose target is in the **main worktree**. Begin every change with
`/leash-start-work` (creates `.worktrees/<slug>` on `<type>/<slug>` from
`main`) and edit files inside that worktree. Concurrent Claude Code sessions
are safe by construction — each has its own worktree; none may touch the main
tree. Finish with `/leash-finish-work`. For a one-off main-tree edit, run
`python -m scripts.harness.allow_main_write "<reason>"` (audited in
`.harness/exceptions.log`) and retry.

**The session stays rooted in the main checkout.** Only edit *targets* live
under `.worktrees/<slug>`; never launch or relocate a Claude Code session
inside the worktree (no `EnterWorktree`, no opening the worktree folder as a
workspace). Session history is keyed to the session's root directory, so a
session rooted in a worktree loses its history when the worktree is removed.
A `SessionStart` hook (`session_root_guard`) warns when this happens.
